[package]
name = "hash-core"
description = "Core data structures to be used for implementing Chess engines, part of the Hash Chess Engine"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dev-dependencies]
criterion = "0.5.1"

[[bench]]
name = "perft"
harness = false

[[bench]]
name = "fen"
harness = false

[build-dependencies]
hash-bootstrap = { path = "../hash-bootstrap" }
rustifact = "0.9.0"
rand = "0.8.5"

[dependencies]
rustifact = "0.9.0"
arrayvec = "0.7.4"
hash-bootstrap = { path = "../hash-bootstrap" }
